const config = {
  theme: {
    extend: {
      colors: {
        primary_gray: "#5C5C5C",
        bg_gray: "#FAFAFA",
        green: "#A1CDA8",
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
